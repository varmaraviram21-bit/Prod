VERSION 2 — BUILT-IN FUNCTIONS

This is a standalone module designed to connect later with Version 1.

Includes 20 built-in functions:
len, type, int, float, str, bool, sum, min, max, sorted, reversed,
enumerate, zip, any, all, map, filter, abs, round, isinstance.

Learning flow:
Definition -> Syntax -> Use cases -> Example -> Interview focus

Practice:
- MCQ
- Output prediction
- Code-writing questions
- Immediate feedback
- Progressive hints
- Mixed interview questions using earlier functions

The code-writing runner is intentionally a lightweight browser-side checker in V2.
A real Python interpreter/test-case runner can be added in a later version.
